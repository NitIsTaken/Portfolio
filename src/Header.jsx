function Header() {
    return(
        <header>
            <h1>Portfolio de Martin</h1>
            <nav>
                <a href="#about">A propos</a>
                <a href="#contact">Contact</a>
            </nav>
        </header>
    );    
}

export default Header;